import MOptionGroup from '../select/src/option-group'

/* istanbul ignore next */
MOptionGroup.install = function (Vue) {
    Vue.component(MOptionGroup.name, MOptionGroup)
}

export default MOptionGroup
