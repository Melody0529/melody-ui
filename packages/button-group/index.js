import MButtonGroup from '../button/src/button-group'

/* istanbul ignore next */
MButtonGroup.install = function (Vue) {
    Vue.component(MButtonGroup.name, MButtonGroup)
}

export default MButtonGroup
