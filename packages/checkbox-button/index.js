import MCheckboxButton from '../checkbox/src/checkbox-button.vue'

/* istanbul ignore next */
MCheckboxButton.install = function (Vue) {
    Vue.component(MCheckboxButton.name, MCheckboxButton)
}

export default MCheckboxButton
